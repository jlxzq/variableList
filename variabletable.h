#ifndef VARIABLETABLE_H
#define VARIABLETABLE_H

#include <QMainWindow>

class variableTable : public QMainWindow {
Q_OBJECT

public:
    variableTable(QWidget *parent = nullptr);

    ~variableTable();
};

#endif // VARIABLETABLE_H
